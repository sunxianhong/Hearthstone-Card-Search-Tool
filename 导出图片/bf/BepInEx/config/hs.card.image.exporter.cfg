## Settings file was created by plugin HsCardImageExporter v0.1.0
## Plugin GUID: hs.card.image.exporter

[General]

## 是否在启动后执行图片导出。
# Setting type: Boolean
# Default value: true
EnableExport = true

## 是否导出完整卡牌图片。
# Setting type: Boolean
# Default value: true
ExportCards = true

## 是否导出狂野、标准、休闲三个格式模式图片。
# Setting type: Boolean
# Default value: true
ExportFormatModes = true

## 完整卡牌 PNG 输出目录。
# Setting type: String
# Default value: C:\Program Files (x86)\Hearthstone\BepInEx\HsCardExport\cards
OutputDir = C:\Program Files (x86)\Hearthstone\BepInEx\HsCardExport\cards

## 格式模式 PNG 输出目录。
# Setting type: String
# Default value: C:\Program Files (x86)\Hearthstone\BepInEx\HsCardExport\modes
ModeOutputDir = C:\Program Files (x86)\Hearthstone\BepInEx\HsCardExport\modes

## 本次最多导出的卡牌数量。0 表示全部。
# Setting type: Int32
# Default value: 0
MaxCount = 0

## 内部渲染宽度。
# Setting type: Int32
# Default value: 1536
RenderWidth = 1536

## 内部渲染高度。
# Setting type: Int32
# Default value: 2304
RenderHeight = 2304

## 列表图宽度。
# Setting type: Int32
# Default value: 512
ThumbWidth = 512

## 列表图高度。
# Setting type: Int32
# Default value: 768
ThumbHeight = 768

## 格式模式图片宽度。
# Setting type: Int32
# Default value: 512
ModeImageWidth = 512

## 格式模式图片高度。
# Setting type: Int32
# Default value: 512
ModeImageHeight = 512

## 是否导出 detail 详情图。
# Setting type: Boolean
# Default value: true
ExportDetail = false

## 详情图宽度。
# Setting type: Int32
# Default value: 1024
DetailWidth = 1024

## 详情图高度。
# Setting type: Int32
# Default value: 1536
DetailHeight = 1536

## 是否跳过已存在的图片文件。
# Setting type: Boolean
# Default value: true
SkipExisting = true

## 导出完成后是否自动关闭导出开关。
# Setting type: Boolean
# Default value: true
AutoDisableWhenDone = true

## 相机取景边界额外保留的比例留白。
# Setting type: Single
# Default value: 0.08
BoundsPaddingRatio = 0.08

## 自动裁透明边后保留的安全边距像素。
# Setting type: Int32
# Default value: 6
TrimPadding = 6

## 是否先按渲染类别导出少量样本图。
# Setting type: Boolean
# Default value: true
PreviewMode = true

## 预览模式下每个渲染类别导出的样本数量。
# Setting type: Int32
# Default value: 4
PreviewPerStrategy = 4

## 按卡包筛选导出范围。支持目录名、枚举名或显示名，多个值可用逗号分隔。
# Setting type: String
# Default value: 
CardSetFilter = 

## 按卡牌 ID 筛选导出范围。支持完整 ID 或片段，多个值可用逗号分隔。
# Setting type: String
# Default value: 
CardIdFilter = 

## 排除指定的 CardSet。支持目录名、枚举名、显示名或数值，多个值可用逗号分隔。
# Setting type: String
# Default value: 
ExcludedCardSets = 1453_BATTLEGROUNDS,1586_LETTUCE,1705_MERCENARIES_DEV,0018_TB_UNKNOWN,1646_VANILLA,0016_CREDITS_UNKNOWN

## 排除指定的 CardType。支持数值或枚举名，多个值可用逗号分隔。
# Setting type: String
# Default value: 
ExcludedCardTypes = 6

## 排除指定的 SpellSchool 值。支持数值或枚举名，多个值可用逗号分隔。
# Setting type: String
# Default value: 
ExcludedSpellSchools = 9

## 按职业补足多职业卡的导出上下文。支持职业枚举名、数值或当前语言显示名。留空时自动尝试读取当前对局/收藏页职业上下文。
# Setting type: String
# Default value: 
CardClassContext = 

